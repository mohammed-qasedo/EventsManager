import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  starContainer: {
    width: 30,
    height: 30,
    marginHorizontal: 10,
  },
  starImg: {
    width: '100%',
    height: '100%',
  },
});

export default styles;
